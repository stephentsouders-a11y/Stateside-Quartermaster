import { chromium } from 'playwright';import fs from 'fs';import path from 'path';
const BASE='https://www.statesideqm.com',THEME='158900125851',key=process.env.KEY,label=process.env.LABEL,route=process.env.ROUTE,OUT='sep18-sg-lazy-retest-'+key;fs.mkdirSync(path.join(OUT,'screenshots'),{recursive:true});
const b=await chromium.launch({headless:true});const c=await b.newContext({viewport:{width:1440,height:1000}});await c.addInitScript(()=>{window.__sqLcp=0;try{new PerformanceObserver(l=>{for(const e of l.getEntries())window.__sqLcp=Math.max(window.__sqLcp,e.startTime||0)}).observe({type:'largest-contentful-paint',buffered:true})}catch{}});
const p=await c.newPage();p.setDefaultNavigationTimeout(70000);const u=new URL(route,BASE);u.searchParams.set('preview_theme_id',THEME);let resp=null,error=null;
try{resp=await p.goto(u.href,{waitUntil:'domcontentloaded',timeout:70000});await p.waitForLoadState('load',{timeout:16000}).catch(()=>{});await p.waitForTimeout(1600)}catch(e){error=String(e?.message||e)}
const row=await p.evaluate(({label,route,themeId})=>{const n=performance.getEntriesByType('navigation')[0],rs=performance.getEntriesByType('resource'),f=performance.getEntriesByName('first-contentful-paint')[0],imgs=[...document.images],grid=document.querySelector('[data-testid="product-grid"]');const first=[...grid?.querySelectorAll('.product-grid__item')||[]].slice(0,8).map(x=>({img:x.querySelector('img')?.currentSrc||'',nw:x.querySelector('img')?.naturalWidth||0,loading:x.querySelector('img')?.loading||''}));return{label,route,theme:{id:String(window.Shopify?.theme?.id||''),name:String(window.Shopify?.theme?.name||'')},ttfbMs:Math.round(n?.responseStart||0),dclMs:Math.round(n?.domContentLoadedEventEnd||0),loadMs:Math.round(n?.loadEventEnd||0),fcpMs:Math.round(f?.startTime||0),lcpMs:Math.round(window.__sqLcp||0),resources:rs.length,transferBytes:rs.reduce((s,r)=>s+(r.transferSize||0),0),imageResources:rs.filter(r=>r.initiatorType==='img').length,imageTransfer:rs.filter(r=>r.initiatorType==='img').reduce((s,r)=>s+(r.transferSize||0),0),domNodes:document.getElementsByTagName('*').length,scripts:document.scripts.length,images:imgs.length,loadedImages:imgs.filter(i=>i.complete&&i.naturalWidth>0).length,eagerImages:imgs.filter(i=>i.loading==='eager').length,lazyImages:imgs.filter(i=>i.loading==='lazy').length,brokenImages:imgs.filter(i=>i.complete&&i.naturalWidth===0).length,cards:grid?.querySelectorAll('.product-grid__item').length||0,first,previewOk:String(window.Shopify?.theme?.id||'')===themeId,h1:(document.querySelector('h1')?.textContent||'').trim()}},{label,route,themeId:THEME});row.httpStatus=resp?.status()??null;row.error=error;fs.writeFileSync(path.join(OUT,'result.json'),JSON.stringify(row,null,2));await p.screenshot({path:path.join(OUT,'screenshots','initial.png'),fullPage:false});console.log('SGLAZYRETEST',JSON.stringify({...row,transferKB:Math.round((row.transferBytes||0)/1024),imageKB:Math.round((row.imageTransfer||0)/1024)}));if(row.httpStatus!==200||!row.previewOk||error||row.brokenImages)process.exitCode=2;await c.close();await b.close();