import { chromium } from 'playwright'; import fs from 'fs'; import path from 'path';
const BASE='https://www.statesideqm.com',THEME='158900125851',key=process.env.KEY,label=process.env.LABEL,route=process.env.ROUTE,kind=process.env.KIND,OUT='sep18-pbtl-scope-verify-'+key;fs.mkdirSync(path.join(OUT,'screenshots'),{recursive:true});
const b=await chromium.launch({headless:true});const c=await b.newContext({viewport:{width:1440,height:1000}});await c.addInitScript(()=>{window.__sqLcp=0;try{new PerformanceObserver(l=>{for(const e of l.getEntries())window.__sqLcp=Math.max(window.__sqLcp,e.startTime||0)}).observe({type:'largest-contentful-paint',buffered:true})}catch{}});const p=await c.newPage();p.setDefaultNavigationTimeout(70000);
const u=new URL(route,BASE);u.searchParams.set('preview_theme_id',THEME);let resp=null,error=null;try{resp=await p.goto(u.href,{waitUntil:'domcontentloaded',timeout:70000});await p.waitForLoadState('load',{timeout:16000}).catch(()=>{});await p.waitForTimeout(1500)}catch(e){error=String(e?.message||e)}
const row=await p.evaluate(({label,route,kind,themeId})=>{const n=performance.getEntriesByType('navigation')[0],rs=performance.getEntriesByType('resource'),f=performance.getEntriesByName('first-contentful-paint')[0];const scripts=[...document.scripts];const has=(sel)=>!!document.querySelector(sel);return{
 label,route,kind,theme:{id:String(window.Shopify?.theme?.id||''),name:String(window.Shopify?.theme?.name||'')},
 ttfbMs:Math.round(n?.responseStart||0),dclMs:Math.round(n?.domContentLoadedEventEnd||0),loadMs:Math.round(n?.loadEventEnd||0),fcpMs:Math.round(f?.startTime||0),lcpMs:Math.round(window.__sqLcp||0),
 resources:rs.length,transferBytes:rs.reduce((s,r)=>s+(r.transferSize||0),0),domNodes:document.getElementsByTagName('*').length,scripts:scripts.length,styles:document.querySelectorAll('style').length,images:document.images.length,
 pbtlFetchScript:has('script[data-sq-pbtl-semantic-resilience-v17]'),pbtlImagePerf:has('script[data-sq-pbtl-image-performance-v10]'),pbtlShopAll:has('script[data-sq-pbtl-shop-all-fast-v3]'),pbtlFinal:has('script[data-sq-pbtl-final-audit-fixes-v5]'),pbtlVisual:has('script[data-sq-pbtl-service-image-enforcer]'),stateGuardFetch:has('script[data-sq-state-guard-fetch-resilience-v2]'),
 pbtlRoot:has('[data-sqbfl-root]'),pbtlView:has('[data-sqbfl-view]'),pbtlCards:document.querySelectorAll('.sqbfl-card,.sq-bfl-card').length,stateGuardRoot:has('[data-sq-state-guard-root]'),stateGuardView:has('[data-sqsg-view]'),rail:has('[data-sq-collection-rail]'),
 h1:(document.querySelector('h1')?.textContent||'').replace(/\s+/g,' ').trim(),previewOk:String(window.Shopify?.theme?.id||'')===themeId
}}, {label,route,kind,themeId:THEME});
row.httpStatus=resp?.status()??null;row.error=error;fs.writeFileSync(path.join(OUT,'result.json'),JSON.stringify(row,null,2));await p.screenshot({path:path.join(OUT,'screenshots','result.png'),fullPage:false}).catch(()=>{});console.log('PBTLSCOPE',JSON.stringify({...row,transferKB:Math.round((row.transferBytes||0)/1024)}));if(row.httpStatus!==200||!row.previewOk||error)process.exitCode=2;await c.close();await b.close();