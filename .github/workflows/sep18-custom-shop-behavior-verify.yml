name: Sep18 Custom Shop behavior-only verification
on:
  push:
    paths:
      - '.github/workflows/sep18-custom-shop-behavior-verify.yml'
      - '.github/scripts/sep18-custom-shop-behavior-verify.mjs'
  workflow_dispatch:
permissions: { contents: read }
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '24' }
      - run: |
          npm init -y >/dev/null 2>&1
          npm install playwright@1.55.0 >/dev/null 2>&1
          npx playwright install --with-deps chromium >/dev/null 2>&1
      - run: node .github/scripts/sep18-custom-shop-behavior-verify.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sep18-custom-shop-behavior-verify-${{ github.run_id }}
          path: sep18-custom-shop-behavior-verify/
          retention-days: 14
